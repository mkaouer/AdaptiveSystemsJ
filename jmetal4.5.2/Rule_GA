la solution est:
IF (Interest=Meduim) and ( Simplicity<=0.36 ) THEN ( Interface simple )
  IF (User Experience=Low) and ( Unity<=0.83 ) THEN ( Interface non ordonné )
  IF (Education Level=Meduim) and ( Regularity>=0.33 ) THEN ( Interface centré )
  IF (Education Level=High) and ( Density<=0.44 ) THEN ( Problem81 )
  IF (Age=High) and ( Density>=0.2 ) THEN ( Interface ordonné )
  IF (Interest=High) and ( Grouping>=0.57 ) THEN ( Interface simple )
  IF (Interest=Meduim) and ( Sequence<=0.61 ) THEN ( Interface irregulié )
  IF (User Experience=Low) and ( Density>=0.83 ) THEN ( Guidage faible )
  IF (Interest=High) and ( Grouping<=0.66 ) THEN ( Interface regulié )
  IF (Education Level=Low) and ( Density<=0.85 ) THEN ( Interface non centré )
  IF (Education Level=Meduim) and ( Sequence<=0.74 ) THEN ( Interface non centré )
  IF (Education Level=Meduim) and ( Sequence>=0.63 ) THEN ( Guidage fort )
  IF (Interest=Meduim) and ( Grouping<=0.23 ) THEN ( Problem81 )
  IF (Interest=Meduim) and ( Unity<=0.28 ) THEN ( Problem82 )
  IF (Interest=Low) and ( Regularity<=0.21 ) THEN ( Guidage faible )
  IF (Motivation=High) and ( Simplicity>=0.25 ) THEN ( Problem82 )
  IF (Education Level=Meduim) and ( Density<=0.55 ) THEN ( Interface simple )
  IF (Motivation=Meduim) and ( Density>=0.26 ) THEN ( Interface non chargé )
  IF (Age=High) and ( Simplicity<=0.13 ) THEN ( Interface non centré )
  IF (User Experience=Meduim) and ( Sequence>=0.05 ) THEN ( Interface non centré )
  IF (User Experience=High) and ( Density>=0.78 ) THEN ( Interface ordonné )
  IF (Age=Meduim) and ( Sequence>=0.92 ) THEN ( Interface complexe )
  IF (Interest=Meduim) and ( Grouping>=0.03 ) THEN ( Interface ordonné )
  IF (Interest=Meduim) and ( Density>=0.77 ) THEN ( Interface complexe )
  IF (Motivation=Meduim) and ( Simplicity<=0.08 ) THEN ( Problem81 )
  IF (Education Level=Meduim) and ( Regularity>=0.18 ) THEN ( Problem71 )
  IF (Education Level=Meduim) and ( Sequence>=0.77 ) THEN ( Interface chargé )
  IF (User Experience=High) and ( Density>=0.68 ) THEN ( Interface ordonné )
  IF (Interest=Low) and ( Unity<=0.96 ) THEN ( Interface regulié )
  IF (User Experience=High) and ( Simplicity>=0.9 ) THEN ( Interface non chargé )
  IF (Interest=Low) and ( Density>=0.26 ) THEN ( Interface chargé )
  IF (Education Level=High) and ( Grouping>=0.1 ) THEN ( Problem81 )
  IF (Age=Meduim) and ( Sequence<=0.06 ) THEN ( Interface centré )
  IF (Interest=High) and ( Unity>=0.7 ) THEN ( Interface non ordonné )
  IF (Motivation=Low) and ( Unity>=0.33 ) THEN ( Interface non chargé )
  IF (Interest=Meduim) and ( Sequence<=0.74 ) THEN ( Interface ordonné )
  IF (Education Level=High) and ( Grouping<=0.7 ) THEN ( Interface chargé )
  IF (Education Level=High) and ( Grouping<=0.15 ) THEN ( Interface non centré )
  IF (Education Level=High) and ( Simplicity>=0.93 ) THEN ( Interface regulié )
  IF (Age=High) and ( Sequence>=0.57 ) THEN ( Guidage fort )
  IF (Interest=High) and ( Grouping>=0.14 ) THEN ( Interface centré )
  IF (User Experience=Meduim) and ( Grouping>=0.33 ) THEN ( Guidage faible )
  IF (Age=Meduim) and ( Density<=0.5 ) THEN ( Interface irregulié )
  IF (Age=Meduim) and ( Simplicity>=0.12 ) THEN ( Interface non centré )
  IF (Motivation=Low) and ( Sequence<=0.39 ) THEN ( Interface non ordonné )
  IF (Interest=High) and ( Density<=0.59 ) THEN ( Problem82 )
  IF (Motivation=High) and ( Grouping>=0.68 ) THEN ( Interface non chargé )
  IF (Education Level=Low) and ( Unity>=0.11 ) THEN ( Problem71 )
  IF (Motivation=High) and ( Regularity<=0.93 ) THEN ( Interface chargé )
  IF (Education Level=High) and ( Sequence>=0.95 ) THEN ( Interface centré )
  
